// Ignore me

procedure empty begin
end

// @todo: allow for standalone expressions inside code blocks
procedure main begin
   result : i32 = -1
   
   if result < 0 then
      new-result := result -= result
   end

   return result
end

